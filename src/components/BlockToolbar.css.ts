import styled from "styled-components";

export const Container = styled.div`
  display: inline-flex;
  background: var(--color-base);
  color: var(--bg-base);
  border-radius: 6px;
  padding: 0 10px;
`;
