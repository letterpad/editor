import styled from "styled-components";

export const Container = styled.div`
  display: inline-flex;
  background: var(--color-base);
  color: var(--bg-base);
  border-radius: 2px;
  padding: 4px 14px;
`;
